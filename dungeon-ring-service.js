(function(){'use strict';const SERVICE='Kiếm / Súng / Phụ kiện',NAME='Lấy CDK A-Z (đã có Tushita và Yama)',PRICE=20000;let n=0;function add(){if(typeof catalog==='undefined'||!catalog||!Array.isArray(catalog[SERVICE]))return false;if(!catalog[SERVICE].some(x=>Array.isArray(x)&&x[0]===NAME))catalog[SERVICE].push([NAME,PRICE]);if(typeof renderServices==='function')renderServices();return true}function retry(){if(!add()&&n++<200)setTimeout(retry,100)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',retry);else retry();window.addEventListener('load',()=>{setTimeout(retry,300);setTimeout(retry,1000)});})();