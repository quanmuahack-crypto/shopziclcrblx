name: Emergency fix shop loading

on:
  push:
    paths:
      - '.github/workflows/emergency-fix-shop-load.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repair malformed UGPhone modal markup
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          bad = '<div id="ugOrderModal"<div id="ugOrderModal" class="modal">'
          good = '<div id="ugOrderModal" class="modal">'
          if bad in s:
              s = s.replace(bad, good, 1)
          p.write_text(s, encoding='utf-8')
          PY
      - name: Commit repair
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'fix: repair malformed UGPhone modal markup'
          git push
      - name: Remove one-time workflow
        if: always()
        run: |
          git rm -f .github/workflows/emergency-fix-shop-load.yml || true
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'chore: remove emergency shop loading workflow' || true
          git push || true
